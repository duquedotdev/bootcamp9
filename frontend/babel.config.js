module.exports = {
  presets: [
    "@babel/preset-env", //Transforma funcionalidades do JS que o browser não entende.
    "@babel/preset-react" //Transforma funcionalidades do React que o browser não entende.
  ],
};